#!/usr/bin/python3

def square_matrix_simple(matrix=[]):
    for row in matrix:
        for i, num in matrix(row):
            new_matrix = ({i**2}. format (num))
        for i, num in matrix(set):
            new_matrix = matrix
    return new_matrix