#!/usr/bin/python3
def best_score(a_dictionary):

    for int in a_dictionary:
        if int in a_dictionary:
          best_key = max(a_dictionary, 'key', 'Value')
          return best_score
        
        else:
           return None
        